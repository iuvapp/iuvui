---
title: Overview
description: The current package, source, Registry, styles, icons, and brand surface for iuvui.
---

iuvui is an ownable React UI system. It pairs maintained packages with an
owned-source workflow, so a team can choose the level of implementation it
wants to maintain. The public website, documentation, catalog, and Registry
all live in the same TanStack Start application. There is no separate
documentation application or Cloudflare Worker.

Every component is free, including complex components. Package mode and source
mode are two delivery forms of the same component contract; neither changes
the accessibility or behavioral baseline.

## What is available today

| Surface            | Current availability                                                                                                                                                                                   | Start here                                      |
| ------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------- |
| React components   | Button, Card, Dialog, Input, Label, Separator, Text Field, and Textarea are on the canonical package path. The last npm `0.0.1` release still contains only Button, Text Field, Dialog, and Separator. | [Components](/docs/components)                  |
| Component variants | Button has a documented free variant, size, and radius contract.                                                                                                                                       | [Variants](/docs/components/variants)           |
| Styles and tokens  | Free semantic tokens and precompiled styles are available, with release state documented per entry point.                                                                                              | [Styles](/docs/styles)                          |
| Icons              | `@iuvui/icons@0.0.1` contains the published Close and Search React icons.                                                                                                                              | [Icons](/docs/icons)                            |
| Source delivery    | The CLI workflow is verified locally, but the CLI is not yet published to npm.                                                                                                                         | [Source delivery](/docs/guides/source-delivery) |
| Public Registry    | Button, Card, Input, Label, Separator, and Textarea have generated public Registry JSON in this repository.                                                                                            | [Registry](/docs/guides/registry)               |

## Choose a delivery mode

### Package mode

The last public npm release is `0.0.1` and contains Button, Text Field, Dialog,
and Separator. The canonical workspace package now also exports Card, Input,
Label, and Textarea on the same path:

```bash
pnpm add @iuvui/react @iuvui/styles
```

Card, Input, Label, and Textarea package exports and CSS are verified locally
and have matching public Registry JSON in this repository. They are not part of
the immutable npm `0.0.1` artifacts. A later authorized `0.0.x` publication is
required before they can be installed from npm.

### Source mode

The CLI installs owned component source under `components/iuv-ui`. Its source
workflow has been verified locally with a packed tarball, but `@iuvui/cli` is
not yet published to npm. Public `pnpm dlx` and global-install commands remain
unavailable until an explicitly authorized release.

## Documentation map

- [Components](/docs/components) documents each current component and its
  delivery state.
- [Variants](/docs/components/variants) records the current public Button
  variant contract and customization boundary.
- [Styles](/docs/styles) explains tokens, CSS entry points, theming, and the
  future Style Pack boundary.
- [Icons](/docs/icons) lists the currently published React icon entries and
  their SVG accessibility contract.
- [Brand](/docs/brand) records the current name, product language, and public
  brand-asset boundary.
- [Guides](/docs/guides) covers package delivery, source delivery, and the
  Registry.
