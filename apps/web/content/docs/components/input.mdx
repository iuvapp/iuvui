---
title: Input
description: A native single-line input with semantic focus, invalid, and disabled styling.
---

```tsx
import { Input } from "@iuvui/react";
```

<ComponentMeta id="input" />

## Preview

<ComponentPreview id="input" />

## Semantics

`Input` preserves native input behavior. Pair it with `Label` using `htmlFor` and `id` for an accessible name.
