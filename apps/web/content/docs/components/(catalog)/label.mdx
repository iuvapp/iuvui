---
title: Label
description: A native label that pairs with form controls through standard HTML semantics.
---

```tsx
import { Input, Label } from "@iuvui/react";
```

<ComponentMeta id="label" />

## Preview

<ComponentPreview id="label" />

## Semantics

Use `htmlFor` on `Label` and the matching `id` on the form control.
